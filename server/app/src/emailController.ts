// import fetch from 'node-fetch';
import GmailApiManager from './utils/gmail';


export const refreshEmails = async (userId: string, pool: any) => {
    // get user's gmail auth token from db

    // check if authtoken is still valid



    
};